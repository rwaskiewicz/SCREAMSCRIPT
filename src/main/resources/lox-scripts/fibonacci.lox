fun fibonacci(n) {
  if (n <= 1) return n;
  return fibonacci(n - 2) + fibonacci(n - 1);
}

var runs = 30;
for (var i = 0; i < runs; i = i + 1) {
  print "Calculation " + (i+1) + "/" + runs + ": " + fibonacci(i);
}