CLASS THING {
  GET_CALLBACK() {
    FUN LOCAL_FUNCTION() {
      PRINT THIS;
    }

    RETURN LOCAL_FUNCTION;
  }
}

VAR CALLBACK = THING().GET_CALLBACK();
CALLBACK();
VAR CALLBACK2 = THING().GET_CALLBACK;
CALLBACK2()();